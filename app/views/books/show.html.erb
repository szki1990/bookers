    <body>
        <p class=created>
            <%= flash[:notice] %>
        </p>
        
        <div class="flex-container">
            <strong>Title:</strong>
            <div id="book-title"><%= @book.title %></div>
        </div>
            <div class="flex-container">
            <strong>Body:</strong>
            <div id="book-body"><%= @book.body %></div>
        </div>
        <%= link_to "Edit", edit_book_path(@book.id) %>|
        <%= link_to "Back", books_path %>
    </body>
